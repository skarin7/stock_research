global:
  scrape_interval: 15s

scrape_configs:
  - job_name: stock-agents
    static_configs:
      # run_agents.py exposes /metrics on METRICS_PORT (default 9100)
      - targets: ["host.docker.internal:9100"]
