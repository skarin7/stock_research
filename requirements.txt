anthropic>=0.40.0
requests>=2.31.0
beautifulsoup4>=4.12.0
pandas>=2.0.0
jinja2>=3.1.0
python-dotenv>=1.0.0
lxml>=4.9.0
yfinance>=0.2.0
google-genai>=2.0.0
pyotp>=2.9.0
growwapi>=1.5.0

# Telegram chat webhook server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0

# Multi-agent system (LangGraph orchestration)
langgraph>=1.0.0
langchain-anthropic>=1.0.0
pydantic>=2.0.0
# OpenRouter provider (optional; only needed when LLM_PROVIDER=openrouter)
openai>=1.0.0
langchain-openai>=0.2.0
# Durable agent + trading state (Postgres); optional — MemorySaver fallback when DATABASE_URL unset
langgraph-checkpoint-postgres>=2.0.0
psycopg[binary]>=3.1.0
sqlalchemy>=2.0.0
cryptography>=42.0.0  # Fernet encryption-at-rest for the cached Groww access token
alembic>=1.13.0
# Observability (self-hosted)
langfuse>=2.0.0
prometheus-client>=0.20.0
# Chat intent router uses OpenRouter (OpenAI-compatible) embeddings via the
# existing `openai` client; cosine math needs numpy (already pulled by pandas,
# pinned here for clarity).
numpy>=1.24.0
croniter>=1.3
